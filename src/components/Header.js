import React from 'react';
import Navbar from './Navbar';
// Remove the import for Header.css
// import './Header.css';

const Header = () => {
  return (
    <header>
      <Navbar />
    </header>
  );
};

export default Header;
